name: Deploy Pedidos

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout codigo
        uses: actions/checkout@v4

      - name: Configurar SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key
          ssh-keyscan -H "${{ secrets.EC2_HOST }}" >> ~/.ssh/known_hosts

      - name: Crear carpeta remota
        run: |
          ssh -i ~/.ssh/ec2_key \
            "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}" \
            "mkdir -p ~/cloudnative/ms-pedidos"

      - name: Copiar proyecto a EC2
        run: |
          scp -i ~/.ssh/ec2_key -r \
            Dockerfile \
            .dockerignore \
            pom.xml \
            src \
            "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:~/cloudnative/ms-pedidos/"

      - name: Desplegar Pedidos
        run: |
          ssh -i ~/.ssh/ec2_key \
            "${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}" << 'EOF'

          cd ~/cloudnative/ms-pedidos

          docker build -t ms-pedidos .

          docker rm -f ms-pedidos || true

          docker run -d \
            --name ms-pedidos \
            --restart unless-stopped \
            --network cloudnative-network \
            -p 8081:8081 \
            --env-file ~/cloudnative/pedidos.env \
            -v /home/ec2-user/cloudnative/wallet-pedidos:/app/wallet:ro \
            ms-pedidos

          docker image prune -f

          EOF